{
  "extends": ["next/core-web-vitals", "next/typescript"],
  "rules": {
    "prefer-const": "off",  // Disable prefer-const rule globally
    "@typescript-eslint/no-unused-vars": "off",  // Disable no-unused-vars globally
    "@typescript-eslint/no-unused-expressions": "off",  // Disable no-unused-expressions globally
    "react-hooks/exhaustive-deps": "off"  // Disable useEffect dependency warnings
  }
}
